import { Component, render, h } from 'preact';

export default class App extends Component {

    render() {
        return (
            <div class="container">
                <h1>Hi</h1>
            </div>
        );
    }
}
