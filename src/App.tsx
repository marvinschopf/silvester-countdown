import { Component, render, h } from 'preact';

export default class App extends Component {

    render() {
        return (
            <div id="app">
                <h1>Hi</h1>
            </div>
        );
    }
}
